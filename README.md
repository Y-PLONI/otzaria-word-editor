# וורד לאוצריא

תוסף לתוכנת [אוצריא](https://github.com/Otzaria/otzaria) שמוסיף לה עורך מסמכים בסגנון Word, לכתיבת חידושי תורה בלי לצאת מהתוכנה. הטקסט הנכתב יושב לצד הספרייה, וכל מקור שמעיינים בו נכנס למסמך בלחיצה אחת.

העורך בנוי על [SuperDoc](https://www.superdoc.dev/) כמנוע DOCX, ורץ כולו בדפדפן — בלי שרת ובלי רשת. הממשק — רצועת הכלים, הלשוניות, שורת המצב והדיאלוגים — נכתב כאן ב‑Vue.

## מה התוסף עושה

**עריכה** — רצועה בשמונה לשוניות. „בית”: לוח, מברשת עיצוב, גופנים, עיצוב תו ופסקה, סגנונות, חיפוש והחלפה. „הוספה”: מעבר עמוד, תמונות, קישורים, תוכן עניינים. „פריסה”: שוליים, כיוון, גודל עמוד, עמודות. „הפניות”: תוכן עניינים, הערות שוליים והערות סיום. „סקירה”: תגובות, מעקב שינויים וקבלה או דחייה שלהם. „תצוגה”: מצב מיקוד, סרגל מידות (אופקי ואנכי, עם גרירת שוליים וכניסות), סימני עיצוב וזום.

**חיבור לאוצריא** — לשונית „אוצריא”: „ציטוט מהקורא” מכניס למסמך את הקטע שפתוח כרגע בקורא יחד עם המקור שלו, ויש חיפוש בספרייה ופתיחת ספר מתוך העורך. בנוסף שלושה תבניות מהירות — חידוש, קושיא ותירוץ.

**קבצים** — מסמך חדש, פתיחת קובץ Word קיים לעריכה, שמירה ושמירה בשם, ייצוא ל‑docx והדפסה. המסמכים נשמרים באחסון של התוסף דרך רכז שמירה שלא כותב תוך כדי עריכה.

בדיקת האיות התורנית מופיעה ברצועה כפקד מנוטרל: המילון עצמו הוא שלב עבודה שטרם נעשה.

## התקנה

הדרך הפשוטה היא דרך חנות התוספים של אוצריא, ושם גם העדכונים מגיעים אוטומטית. להתקנה ידנית מורידים את קובץ ה‑`.otzplugin` מעמוד [Releases](../../releases), ובאוצריא נכנסים להגדרות ← ניהול תוספים ← התקנה מקובץ. נדרשת גרסה 0.9.97 ומעלה. אחרי ההתקנה מופיעה לשונית „וורד לאוצריא” בסרגל הכלים.

מלבד ה‑Releases המתויגים יש גם `v2-dev` — prerelease שמתעדכן בכל קומיט ל‑main, לבדיקות.

## פיתוח

```
npm ci
npm run dev       # שרת פיתוח, עם דמה של מאחז אוצריא
npm run verify    # typecheck, טסטים, build, ושערי dist / boot / fonts / rtl / sdk
```

`npm test` מריץ את חבילת ה‑vitest. הבדיקות אינן מרימות את מנוע ה‑DOCX — הוא דורש workers ו‑canvas אמיתיים — ולכן ריצה חיה נבדקת בשערי Windows המתועדים ב‑[docs/spike-windows.md](docs/spike-windows.md).

## פרסום גרסה חדשה

מקור האמת הוא `version` ב‑[public/manifest.json](public/manifest.json). מעלים אותו, דוחפים ל‑main, והשאר אוטומטי: `.github/workflows/release.yml` בונה, מאמת מול הוולידטור הרשמי של אוצריא, אורז `.otzplugin`, יוצר Release בשם `v<גרסה>` ומפרסם את העדכון לחנות ב‑otzaria.org. אין תגים לדחוף ביד.

אם כבר קיים Release לאותה גרסה, שלב היצירה והפרסום מדולג ורק ערוץ ה‑dev מתעדכן. החנות דוחה עדכון עם מספר גרסה שכבר קיים, ולכן הגרסה חייבת לעלות.

הפרסום לחנות מסתמך על שני סודות בהגדרות המאגר (Settings ← Secrets and variables ← Actions): `OTZARIA_USER` ו‑`OTZARIA_PASSWORD`. בלעדיהם השלב מדולג בשקט, וה‑Release עדיין נוצר כך שאפשר להתקין מהקובץ.

## מבנה המאגר

`index.html` הוא נקודת הכניסה, ו‑`src/` הוא הקוד:

- `src/engine/` — כל המפגש עם SuperDoc. הקוד שמחוץ לתיקייה הזו אינו נוגע במנוע ישירות, ובדיקה אוטומטית שומרת על הגבול.
- `src/host/` — הדיבור עם אוצריא: קבצים, קורא, הגדרות, ערכת נושא, ודמה לפיתוח בדפדפן.
- `src/sessions/` — פתיחת מסמכים, החלפת עורך, ורכז השמירה.
- `src/ui/` — הרצועה, המעטפת, הדיאלוגים והאייקונים.

`public/` הוא מה שנארז כמו שהוא, ובראשו ה‑manifest. `scripts/` הם שערי הבנייה — `check:dist` ואחיו — ש‑`npm run verify` וה‑CI מריצים. `tests/` מחולק ל‑unit, contract ו‑component. `docs/` מכיל את תוכנית העבודה ואת מסמכי ה‑spike, ו‑`ai.md` הוא בירור התשתית והרישוי שממנו הכול התחיל.

## רישיון

התוסף מופץ תחת [GNU Affero General Public License v3.0](LICENSE).

הבחירה ב‑AGPL נובעת מהמנוע: [SuperDoc](https://www.superdoc.dev/) מופץ תחת AGPL‑3.0, ולכן גם התוסף חייב להיות AGPL.

אייקוני הרצועה הם היוצא מן הכלל היחיד לכיוון השני: 65 מתוך 76 האייקונים
ב‑`src/ui/icons/icons.ts` הם ה‑path data המקורי של
[Fluent System Icons](https://github.com/microsoft/fluentui-system-icons) של Microsoft,
שמופצים תחת **MIT**. ה‑MIT מתיר שילוב ב‑AGPL, ומחייב שנוסח הרישיון והקרדיט יופצו עם כל
עותק — ולכן הם באנר `@license` בראש `icons.ts`, ו‑`npm run check:dist` מאמת שהבאנר שרד
את המינימיזציה. לא נוספה תלות: הועתק path data בלבד. הפירוט, כולל טבלת אייקון‑לפקד,
ב‑[THIRD_PARTY_NOTICES.md](THIRD_PARTY_NOTICES.md).

הערה חשובה: מנוע ה‑DOCX של SuperDoc (`@superdoc/docx-engine`) הוא **רכיב קנייני בנפרד**
שאינו קוד פתוח ואינו מכוסה ברישיון הזה. מפתחי SuperDoc אישרו במפורש
([issue #3927](https://github.com/superdoc/docx-editor/issues/3927#issuecomment-5383145303))
שתוסף קוד פתוח תחת AGPLv3 רשאי לארוז ולהפיץ את המנוע וה‑Workers שלו בתוך חבילה אופליין —
בתנאי שהמקור מפורסם וזהה למופץ, שמייבאים `superdoc` בלבד ולא את המנוע ישירות, ושכל הודעות
הרישוי נשמרות. פירוט ב‑[docs/word-plugin-implementation-plan.md](docs/word-plugin-implementation-plan.md) §2.
